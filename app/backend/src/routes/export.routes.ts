import login from './login.routes';
import teams from './teams.routes';
import matcher from './match.routes';
import leaderBoard from './leaderBoard.routes';

const routes = {
  login,
  teams,
  matcher,
  leaderBoard,
};

export default routes;
